<?xml version="1.0" encoding="UTF-8"?>
<ruleset xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" comparisonMethod="maven" xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0 https://www.mojohaus.org/versions-maven-plugin/xsd/rule-2.0.0.xsd">
    <ignoreVersions>
        <!-- Ignore Alpha's, Beta's, release candidates and milestones -->
        <ignoreVersion type="regex">(?i).*Alpha(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*a(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*Beta(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-B(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*RC(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*RC.*(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*CR(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*M(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*preview(?:-?\d+)?</ignoreVersion>
        
        <ignoreVersion>20030211.134440</ignoreVersion>
        <ignoreVersion>20030825.184428</ignoreVersion>
        <ignoreVersion>20021128.082114</ignoreVersion>
        <ignoreVersion>20030821.193421</ignoreVersion>
        <ignoreVersion>20030818.201141</ignoreVersion>
        <ignoreVersion>20030825.183949</ignoreVersion>
        <ignoreVersion>20040102.233541</ignoreVersion>
        <ignoreVersion>20030821.192746</ignoreVersion>
        <ignoreVersion>20031027.000000</ignoreVersion>
        <ignoreVersion>20030818.195203</ignoreVersion>
        <ignoreVersion>20030418.083655</ignoreVersion>
        <ignoreVersion>20030623.172700</ignoreVersion>
        <ignoreVersion>20020520</ignoreVersion>
        <ignoreVersion>20040616</ignoreVersion>
        <ignoreVersion>20020423</ignoreVersion>
        <ignoreVersion>20020501</ignoreVersion>
        <ignoreVersion>99.0-does-not-exist</ignoreVersion>
        <ignoreVersion>3.1.SONATYPE</ignoreVersion>
        <ignoreVersion>3.1-osgi</ignoreVersion>
        <ignoreVersion>3.1-jenkins-1</ignoreVersion>
        <ignoreVersion>3.1-jbossorg-1</ignoreVersion>
        <ignoreVersion>3.1-atlassian-1</ignoreVersion>
        <ignoreVersion>3.1-atlassian-2</ignoreVersion>
        <ignoreVersion>2.1.1.jbossorg-1</ignoreVersion>
    </ignoreVersions>
    
    <rules>
    </rules>
</ruleset>